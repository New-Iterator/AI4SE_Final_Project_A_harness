# REFLECTION.md

## 反思报告：Coding Agent Harness 项目

### 1. Superpowers 技能：哪些有用，哪些多余

brainstorming 是最有用的。它那种一次只问一个问题、每段设计都让你确认的方式，在我刚开始什么都还没想清楚的时候特别管用。比如它问我选什么语言、接什么 LLM、重点做哪个维度，这些问题如果我自己想可能就直接跳过去了，但它逼着我把每个决策的理由都想清楚。最后选 TypeScript 是因为强类型适合状态机，选多供应商抽象是因为 mock 友好，选记忆系统作为重点是因为工程深度最大——这些理由后来在实现中都验证了。

writing-plans 产出的 PLAN 也很关键。22 个 Task，每个都有完整的文件路径、接口签名、测试步骤，甚至代码都写好了。后来 subagent 因为环境问题跑不动的时候，我直接照着 PLAN 手写代码，基本就是"翻译"而不是"设计"。

subagent-driven-development 是让我最头疼的。Task 1 的 subagent 跑了没几分钟就因为 Node.js 没装好挂了，后面再试还是各种环境问题。而且 PLAN 里的代码已经精确到可以直接复制粘贴了，让 subagent 来"理解→实现→测试→评审"完全是多余的流程。最后我直接放弃了 subagent 模式，自己照着 PLAN 手写了所有代码。这个技能的想法是好的，但在 PLAN 足够精确的时候就是形式大于实质。

### 2. TDD 在 AI 协作下是阻碍还是放大器

说实话，红-绿-重构的节奏在 AI 协作下确实被打乱了。AI 一次性就写出完整实现了，你很难让它"先写最少的代码让测试通过"。但 TDD 的另一个价值被放大了——它提供了可复现的验证标准。尤其是 mock LLM 测试，每个测试用例都是确定性的，不依赖网络和真实 LLM，跑一遍就知道框架有没有问题。这个项目里 98 个测试全部用 mock LLM，每次改完代码跑一遍 `npm test`，几秒钟就知道结果。没有这套测试的话，后面那 6 轮对齐循环根本没法做。

### 3. Subagent 能自主运行多久

基本没跑起来。Task 1 就被环境问题卡住了，后面改用手写。但这次经历让我想清楚了一个问题：subagent 的自主性取决于环境的可预测性和 PLAN 的精确度。环境越干净、PLAN 越模糊，subagent 越有价值；环境越复杂、PLAN 越精确，手写越高效。

至于最优 Task 颗粒度，我觉得是 1-3 个文件、有明确接口定义、有测试用例的那种。太大会超出 subagent 的上下文窗口，太小又没独立价值。

### 4. SPEC/PLAN 质量如何影响实现质量

举一个具体例子：会话边界。原始 SPEC 里没有明确定义"会话"是什么——ID 怎么生成、生命周期怎么管理、L2 和 L3 怎么隔离。我在实现记忆系统的时候只能自己临时决定：用 UUID v4 生成 sessionId，会话从 loop 初始化开始到退出结束，L2 按 sessionId 隔离，L3 跨会话共享。如果 SPEC 一开始就写清楚这些，就不会有这种"填空"式的实现。

反过来，context-injector 的 SPEC 就写得很清楚：L2 检索→L3 检索→注入到 system prompt 之后→token 超限触发压缩。这 5 步明确描述让实现变成了纯粹的翻译，没有任何歧义。SPEC 越精确，实现越不需要"设计"；SPEC 越模糊，实现就是在"猜"。

### 5. 最有效的 Prompt/Context 策略

在 PLAN 里嵌入完整代码。这个策略让 PLAN 从"描述做什么"变成了"描述怎么做"，实现阶段不需要理解需求，只需要执行代码。而且代码质量可以在 PLAN 阶段就审查，不用等到实现后再改。对比之下，模糊的 dispatch prompt（"Implement Task N"）会让 AI 浪费大量时间在理解需求上，而这些需求 PLAN 已经回答过了。

### 6. 凭据与分发让我想清楚了什么

写凭据威胁模型的时候，我被迫去想 API Key 的完整攻击面——不只是"放在 .env 里别提交"，还有进程内存、日志、终端历史、Git 历史、备份同步这些我根本没想过的攻击面。后来还加了 sanitizeKey 函数做日志脱敏、zeroKeys 方法做内存置零、pre-commit hook 检查提交内容。这些如果不是课程要求，我大概率不会主动去做。

分发方面，Docker + npm 的组合让我去想"别人怎么在一台全新机器上跑起来"。better-sqlite3 需要原生编译，Docker 能解决这个问题；npm 的 `npm install -g .` 和 `npm link` 让本地安装也更方便。分发不是一个"打包"步骤，是"为使用者设计体验"。

### 7. 如果重做，我会改变什么

第一，开始实现前先做环境检查。Node.js 没装好这种问题不应该在 subagent 跑的时候才发现。

第二，早点做真实 LLM 测试。mock 测试能验证流程正确，但直到用学校 API 真跑的时候才发现 LLM 在 Windows 上输出 `ls` 和 `cat` 这种 Unix 命令，因为系统提示词根本没告诉它当前是 Windows。这种问题 mock 测试发现不了。

第三，PLAN 不用写那么精确的代码。现在的 PLAN 精确到可以直接复制粘贴，失去了让 subagent 发挥价值的机会。如果重做，我会让 PLAN 只描述接口和数据流，不给出完整代码，让 subagent 在接口约束下自己实现。

### 8. 对 Superpowers 的批判

Superpowers 假设了几件事：环境是可控的、PLAN 和 SPEC 是分离的、人类会在每个决策点介入、TDD 的节奏适用于 AI 协作。在我的项目里，这几个假设都不完全成立。

环境问题（Node.js 没装、中文路径导致 vitest 解析失败）直接让 subagent 工作流瘫痪。SPEC 和 PLAN 有大量内容重叠，维护两份文档增加了成本。human-in-the-loop 的设计很好，但有些次要决策（命名、风格）其实不需要人工介入。TDD 的"先红后绿"在 AI 能一次性写出完整实现的时候确实不太适用。

但 Superpowers 的核心价值不在于这些假设是否成立，而在于它守住了最容易被 AI 协作消解的纪律——写 SPEC、写 PLAN、写测试、做评审。没有这套流程，我可能直接就开始写代码了，不会去想凭据威胁模型、不会去做冷启动验证、不会写 98 个测试用例。它是"流程脚手架"，提醒你该做什么，但真正决定质量的还是你在每个环节里投入了多少思考。

### 9. 总结

这个项目让我对课程的核心命题有了自己的答案：当 LLM 能完成大部分编码的时候，工程师的价值不在"写代码"，而在"决定写什么代码"和"验证代码写对了没有"。我写 TypeScript 代码的时间不到总时间的 30%，更多时间花在了设计记忆系统模型、分析威胁模型、调试 CI、配置部署、做 6 轮对齐循环上。这些工作 LLM 能辅助，但不能替代——因为它们需要判断力，而判断力是这门课最想教的东西。

AI 是放大器，它放大的不是你的编码能力，而是你的工程判断力。