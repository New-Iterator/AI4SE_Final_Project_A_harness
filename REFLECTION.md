# REFLECTION.md

## 反思报告：Coding Agent Harness 项目

---

### 1. Superpowers 技能：哪些发挥了最大作用，哪些"形式大于实质"

**最大作用：brainstorming 技能**

brainstorming 是本次项目中价值最高的技能。它的核心设计——一次只问一个问题，逐段设计逐段确认——在需求模糊的初期阶段至关重要。传统的"自顶向下一次性设计"容易产生大量未经验证的假设，而 brainstorming 的增量式确认让每个设计决策都有明确的上下文和理由。具体而言，它帮我明确了几件事：(1) 多供应商抽象的设计决策直接影响了后续 mock LLM 的实现难度；(2) 纯 CLI 的选择排除了前端开发，聚焦了工程精力；(3) 记忆/上下文工程作为重点维度的选择，指导了整个项目的资源分配方向。

**最大作用：writing-plans 技能**

writing-plans 产出的 PLAN 是本次实现的核心资产。每个 Task 包含完整的文件路径、接口签名、TDD 步骤和预期代码，这使实现过程几乎变为"转录"——即使 subagent 因环境问题被 BLOCKED，代码仍然可以直接从 PLAN 中提取。一个值得注意的细节是：PLAN 中每个 Task 的代码都是自包含的——Task 5 的实现者不需要读 Task 4 的代码，只需要知道接口签名。这种"接口先行"的规划设计，与编程中"面向接口编程"的思想一脉相承。

**形式大于实质：subagent-driven-development 技能**

subagent-driven-development 是本项目中最令人失望的技能。它的设计理念——每个 Task 派一个全新 subagent，实施后评审再继续——在理论上很优雅，但在实践中暴露了严重问题：

1. **环境依赖**：每个 subagent 需要独立的环境，但环境问题（如 Node.js 未安装）会导致整个 subagent 被 BLOCKED，而控制器无法直接解决。
2. **上下文开销**：每个 subagent 需要独立的上下文，控制器需要为每个 Task 准备 dispatch prompt，这本身就需要大量精力。
3. **对转录类 Task 过度设计**：当 PLAN 中的代码已经精确到可以直接复制粘贴时，subagent 的"理解→实现→测试→评审"循环完全多余。直接执行 PLAN 中的代码更高效。
4. **评审成本**：每个 Task 的评审需要 controller 生成 diff 文件、派发 reviewer subagent、处理 reviewer 的反馈——对于 22 个 Task，这意味着 44+ 次 subagent 调用。

这个问题并非 subagent-driven-development 的固有缺陷，而是"精确 PLAN"与"灵活 subagent"之间的矛盾。当 PLAN 足够精确时，subagent 的灵活性价值趋近于零，而评审成本保持不变。这让我反思：什么样的 PLAN 适合 subagent-driven？答案可能是——PLAN 只描述"做什么"而不描述"怎么做"的场景。当 PLAN 精确到代码级别时，直接执行更高效。

---

### 2. TDD 在 AI 协作下是阻碍还是放大器

**TDD 是放大器，但需要适配**

在本项目中，TDD 的核心价值不是"防止 bug"（AI 生成的代码 bug 率并不高），而是"提供可复现的验证标准"。这一点在 mock LLM 驱动的测试中尤为明显——每个测试用例都是一段可独立运行的、确定性的验证脚本，不依赖真实 LLM 或网络。

但 TDD 在 AI 协作下也有适配问题：

1. **红-绿-重构的节奏被打乱**：传统 TDD 是先写测试→看到测试失败→写最少代码让测试通过→重构。但在 AI 协作中，AI 倾向于一次性写出"完整"的实现，而不是"最少"的实现。这导致"绿"阶段往往不是"最小化"而是"过度实现"。

2. **测试的"信任"问题**：在 AI 协作中，测试既是验证工具也是信任工具。当 AI 说"6/6 tests pass"时，我需要验证——不仅是数字，而是 (1) 测试是否真的有断言，(2) 测试是否覆盖了边界条件，(3) 测试是否验证了真实行为而非 mock 行为。这就是为什么我在 PLAN 中为每个测试都写了完整的断言代码。

3. **mock LLM 测试的设计**：最重要的设计决策是让 mock LLM 支持"脚本模式"和"回放模式"。脚本模式按顺序消费预设的响应，回放模式按顺序返回录制的响应。这确保了即使没有真实 LLM，每个核心机制都能被确定性地验证。

**结论**：TDD 在 AI 协作中是必要的，但它的价值从"驱动设计"转向了"提供可复现的验证标准"。在 AI 能一次性生成代码的场景下，TDD 的"红"阶段（看到测试失败）的意义被削弱，"绿"阶段和"重构"阶段的价值被保留。

---

### 3. Subagent-driven 工作流：智能体能自主运行多久

**在本项目中，subagent 几乎没有自主运行**

Task 1 的 subagent 在创建了 5 个文件后就被 BLOCKED，原因是 Node.js 未安装。这不是 subagent 的"智能"问题，而是环境问题。但这也暴露了 subagent-driven 工作流的一个核心弱点：**subagent 的自主性依赖于环境的可预测性**。

在理想环境中（所有依赖已安装，PLAN 足够明确，Task 边界清晰），subagent 应该能自主完成一个 Task。但在本项目的中文路径 + 缺少 Node.js 的环境中，subagent 的自主性被严重限制。

**最优 Task 颗粒度**：根据本次经验，最优的 Task 颗粒度是：
- 1-3 个文件
- 有明确的接口定义（输入/输出类型）
- 有完整的测试用例
- 不依赖前序 Task 的实现细节（只依赖接口签名）

这个颗粒度足够小，可以让 subagent 在 2-5 分钟内完成，也足够大，有独立的价值和可验证性。

---

### 4. SPEC/PLAN 质量如何影响实现质量

**一个具体案例：会话边界定义**

在原始 SPEC 中，会话边界没有被明确定义。这导致在实现记忆系统时，我不得不临时决定：会话 ID 如何生成（UUID v4）、生命周期如何管理（创建于 loop 初始化、销毁于 loop 退出）、L2 如何按会话隔离（通过 sessionId 字段）、L3 是否跨会话共享（是）。

如果在 SPEC 阶段没有补充这个定义，实现者（subagent 或我自己）会做出不同的假设，可能导致记忆系统的行为不一致。例如，一个 subagent 可能认为每个 `harness run` 命令是新会话，另一个可能认为每个 LLM 调用是新会话。

**另一个案例：语境注入策略**

SPEC 中明确描述了 context-injector 的行为：从 L2 检索→从 L3 检索→注入 system prompt 之后→token 超限时触发 compressor。这 5 个步骤的明确描述，使实现变成了"翻译"而非"设计"。如果没有这个描述，实现者可能会做出不同的选择（注入位置、注入顺序、token 估算方式），导致不可预测的行为。

**结论**：SPEC 的质量直接决定了实现的"确定度"。SPEC 越精确，实现越可能是"翻译"而非"设计"；SPEC 越模糊，实现越需要"填空"，而"填空"的质量取决于实现者的判断力。

---

### 5. 最有效的 Prompt/Context 策略

**最有效的策略：在 PLAN 中嵌入完整代码**

这是本次项目中最关键的发现。传统的 PLAN 描述"做什么"（create a config loader, write tests），而本项目的 PLAN 描述了"怎么做"（完整的代码、精确的断言、具体的命令）。这意味着：

1. 实现者不需要理解需求——只需要执行代码
2. 代码质量可以在 PLAN 阶段预审，而非在实现后审查
3. 实现结果与预期的偏差趋近于零

**为什么有效**：因为 PLAN 阶段的上下文是最充足的——此时已经完成了 brainstorming 和 SPEC 撰写，所有设计决策都清晰。而实现阶段的上下文是最碎片化的——subagent 只看到单个 Task，不了解全局。因此，在 PLAN 阶段把"怎么做"想清楚，比在实现阶段让 subagent 自己"想"更高效。

**不那么有效的策略**：在 dispatch prompt 中写"Implement Task N: do X"——这种模糊的指令会导致 subagent 浪费时间在理解需求上，而 PLAN 已经回答了所有需求相关的问题。

---

### 6. 凭据与分发：迫使你想清楚了什么

**凭据管理**：写威胁模型的过程迫使我系统性思考 API Key 的完整攻击面。从"把 key 放在 .env 里"到"分析 8 个攻击面并提供对策"，这个思维转变是凭据安全要求带来的。特别是"进程间通信"和"备份与同步"这两个攻击面，在没有写威胁模型之前，我根本没有想到。

**分发**：Docker + npm 的组合选择，迫使我思考"别人如何运行这个项目"。Docker 解决了 better-sqlite3 原生编译的跨平台问题，npm 解决了"一个命令安装"的便利性问题。这个决策让我意识到：分发不是"打包"一个步骤，而是"为使用者设计体验"。

**实际落地**：由于 better-sqlite3 需要原生编译，npm 安装在某些环境下可能失败。Docker 镜像预装编译工具，解决了这个问题。这是"分发"要求迫使我想清楚的一个具体问题。

---

### 7. 如果重做，我会改变什么

1. **环境预检**：在开始实现前，先检查 Node.js、npm 等工具是否可用，并自动配置 PATH。这避免了 Task 1 被 BLOCKED 的尴尬。

2. **PLAN 的精确度权衡**：当前 PLAN 的代码精确到"复制粘贴"级别，这减少了实现中的不确定性，但也失去了让 subagent 发挥价值的机会。如果重做，我会让 PLAN 描述接口和数据流，但不给出完整代码——让 subagent 在接口约束下自主实现，评审时重点检查接口合规性。

3. **更早地触发冷启动验证**：在实现前用一个不同的 agent 读取 SPEC 并尝试实现 1-2 个 Task，这能发现 SPEC 中的模糊之处。本项目中，这一步被跳过了，但回顾来看，它可能会提前发现"会话边界"和"mock LLM 匹配策略"的模糊性。

4. **记忆系统的测试覆盖**：当前记忆系统的测试覆盖了基本的 CRUD 和检索，但缺少跨会话记忆一致性、大量条目下的检索性能、记忆污染检测等场景的测试。如果重做，我会在重点维度上投入更多测试精力。

---

### 8. 对 Superpowers 方法论的批判

**Superpowers 假设了什么？**

1. **假设环境是可控的**：subagent-driven-development 假设 subagent 的工作环境是预配置好的，但现实中环境问题（依赖缺失、路径问题）是常态。

2. **假设 PLAN 和 SPEC 是分离的**：brainstorming 产生 SPEC，writing-plans 产生 PLAN。但在实践中，SPEC 和 PLAN 的内容有大量重叠（架构描述、数据模型、接口定义）。这个分离增加了维护成本。

3. **假设人类会做所有决策**：brainstorming 的"逐段确认"设计假设人类在每个设计决策点都在场并且愿意做出决策。但在实践中，人类可能希望 AI 直接做出次要决策（如文件命名、代码风格），只在关键决策点介入。

4. **假设 TDD 的"红-绿-重构"节奏适用于 AI 协作**：如前面所述，AI 倾向于一次性写出完整实现，传统的"先红后绿"节奏被打破。

**这些假设在我的项目里成立吗？**

| 假设 | 成立程度 | 实际表现 |
|------|---------|---------|
| 环境可控 | 不成立 | Node.js 未安装、中文路径导致 vitest 解析失败 |
| PLAN/SPEC 分离 | 部分成立 | 有重叠但各自的定位不同（SPEC 给人类看，PLAN 给 AI 看） |
| 人类做所有决策 | 不成立 | 部分决策（如 mock LLM 匹配策略）更适合在实现时根据实际情况决定 |
| TDD 节奏 | 部分成立 | 红-绿-重构的节奏被打破，但"先写测试"的原则仍然有效 |

**最根本的批判**：Superpowers 的设计哲学是"流程纪律 > 灵活应变"。这在理论上是对的——AI 协作中最容易丢失的就是纪律。但在实践中，过度强调流程会导致"为了遵守流程而工作"而非"为了产出质量而工作"。例如，subagent-driven-development 要求每个 Task 都经过 implementer → reviewer → fix → re-review 的循环，但当 PLAN 已经精确到代码级别时，这个循环是完全冗余的。

**我的结论**：Superpowers 是一个有价值的"流程脚手架"，但它不是银弹。它的价值在于"提醒你该做什么"（写 SPEC、写 PLAN、写测试、做评审），而不是"替你做什么"。真正决定项目质量的，是你在每个环节中投入的思考深度——SPEC 是否覆盖了边界条件，PLAN 是否考虑了实现细节，测试是否验证了真实行为，评审是否发现了真正的问题。

---

### 9. 对整个 AI4SE 课程的思考

这门课的核心命题是"当 LLM 能完成大部分编码工作时，一个工程师的真正价值在哪里"。通过本次项目，我的答案是：

**工程师的价值不在"编码"而在"决定编码什么"和"验证编码对了吗"**。

具体来说，本次项目中我做的"编码"工作（写 TypeScript 代码）几乎全部由 SPEC 和 PLAN 驱动——代码本身是 SPEC/PLAN 的翻译。而我真正投入思考的地方是：(1) 记忆系统的三层模型设计；(2) 凭据的威胁模型分析；(3) mock LLM 的匹配策略设计；(4) 测试用例的覆盖范围设计；(5) 分发方案的选择和实际验证。

这些"设计"和"验证"的工作，LLM 可以辅助但不能替代——因为它们需要判断力、经验和权衡，而这些正是工程师的核心竞争力。

---

### 10. CI/CD 与部署的实战反思

CI/CD 的配置和调试是本次项目中"意外耗时最多"的环节。表面上看，`.gitlab-ci.yml` 只有 30 行，似乎几分钟就能写完。但实际遇到的问题是：

1. **镜像选择**：`node:20-alpine` 缺少编译原生模块（better-sqlite3、keytar）的 build 工具，导致 `npm ci` 失败。这个问题在 Windows 本地开发环境中不会出现（因为已预装编译工具），但到了 CI 的干净容器中就会暴露。这让我意识到 CI 配置的"环境一致性"不是自动保证的——需要主动验证。

2. **认证问题**：GitLab 的 Git Credential Manager 在 Windows 环境下无法自动认证，需要手动创建 Personal Access Token。这个"人机认证"的环节在 AI 协作中完全无法自动化——它需要人类在浏览器中操作。

3. **部署平台的选择**：Render 提供了最简单的部署体验（GitHub 导入 → 自动检测 Node.js → 填写 Build/Start Command），但免费版的休眠机制意味着首次访问需要等待 1-2 分钟。这个"用户体验"问题在设计阶段很难预料——只有实际部署后才能发现。

**教训**：CI/CD 和部署不是"最后一步"，而是"持续验证"——应该在整个开发过程中持续运行，而不是等到项目结束再配置。

---

### 11. 跨平台协作的挑战

本次项目涉及多个平台：Windows（本地开发）、Linux（GitLab CI Docker 镜像）、Render（云端部署）。每个平台都有其独特的问题：

1. **Windows**：中文路径导致 vitest 模块解析失败（深度 3 的模块无法解析），PowerShell 不支持 `&&` 命令链，SSL 证书验证问题
2. **Linux CI**：Alpine 镜像缺少原生编译工具，需要手动安装或切换到 Debian 基础镜像
3. **Render**：免费版休眠机制，首次访问延迟

这些问题的共同特征是：它们不是"代码逻辑"问题，而是"环境配置"问题。AI 可以帮你写代码，但很难帮你解决环境问题——因为环境问题通常需要物理访问（安装软件、配置网络、创建 Token）。

**教训**：在 AI 协作项目中，应该尽早搭建 CI/CD 和部署环境，让环境问题尽早暴露。如果在项目末期才配置 CI/CD，可能发现大量环境不兼容问题，导致返工。

---

### 12. 交付物清单的"约束力"反思

课程要求中有一份详细的"交付物清单"（9 项），这份清单在项目初期看起来是"形式要求"，但在项目后期发挥了关键作用：

1. **`.gitlab-ci.yml` 必须有 `unit-test` job**：这个要求迫使我学习和配置 GitLab CI，而不仅仅是写 GitHub Actions（我原来更熟悉的平台）
2. **线上部署 URL**：这个要求迫使我实际部署项目，而不是只停留在"本地能跑"的阶段
3. **机制演示**：这个要求迫使我为每个核心机制编写可运行的演示脚本，而不是只依赖单元测试

如果这份清单是在项目初期给我的，我可能会觉得"又多了一堆要写的东西"。但实际体验是，这些要求不是在"增加工作量"，而是在"增加交付质量"——它们确保项目不只是"代码能跑"，而是"可以交付给他人使用"。

**教训**：交付物清单是"设计约束"的一种形式。好的设计约束不会限制创造力，而是引导注意力到真正重要的地方。

---

### 13. 最终总结

本次 Coding Agent Harness 项目，从 brainstorming 到线上部署，整个过程让我对 AI4SE 课程的核心命题有了自己的答案：

**AI 改变了软件开发的速度，但没有改变软件开发的质量要求。**

AI 可以在一小时内生成一个完整的项目，但 SPEC 的边界条件、PLAN 的 Task 粒度、测试的覆盖范围、CI/CD 的环境一致性、部署的可用性——这些质量要求不会因为 AI 的参与而降低。相反，AI 的速度让你有更多时间去思考这些"质量"问题。

在整个项目中，我"写代码"的时间不到总时间的 30%。其余 70% 的时间花在了：设计记忆系统模型、分析凭据威胁模型、调试 CI/CD 环境、配置部署平台、撰写文档。这些工作才是"软件工程"的核心——不是"我能写多少代码"，而是"我能确保多少质量"。

如果让我用一句话总结这个项目的收获，那就是：**"AI 是放大器，它放大的不是你的编码能力，而是你的工程判断力。如果你的判断力不够，AI 会放大你的错误；如果你的判断力足够，AI 会放大你的价值。"**